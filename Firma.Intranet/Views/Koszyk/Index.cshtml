@model IEnumerable<Firma.Data.Data.Sklep.Koszyk>
@{
    ViewData["Title"] = "Koszyki";
}
<h1>Lista koszyków</h1>
<a asp-action="Create" class="btn btn-primary mb-3">
    <i class="bi bi-plus-circle"></i> Nowy koszyk
</a>
<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>Data utworzenia</th>
            <th>Użytkownik</th>
            <th>Liczba pozycji</th>
            <th>Czy zamowiony</th>
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.DataUtworzenia.ToString("g")</td>
                <td>@item.Uzytkownik?.Imie @item.Uzytkownik?.Nazwisko</td>
                <td>@item.Pozycje.Count</td>
                <td>@item.CzyZamowiony</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdKoszyka" class="btn btn-olive btn-sm me-1"><i
                            class="bi bi-pencil-square"></i> Edytuj</a>
                    <a asp-action="Details" asp-route-id="@item.IdKoszyka" class="btn btn-slate btn-sm me-1"><i
                            class="bi bi-info-circle"></i> Szczegóły</a>
                    <form asp-action="Delete" asp-route-id="@item.IdKoszyka" method="post" class="d-inline">
                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Na pewno usunąć?')"><i
                                class="bi bi-trash"></i>Usuń</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
